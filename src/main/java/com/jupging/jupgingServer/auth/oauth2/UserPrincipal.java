package com.jupging.jupgingServer.auth.oauth2;

public class UserPrincipal {
    // 이게 있어야 @AuthenticationPrincipal 를 쓸 수 있는듯
    // SecurityContextHolder.getContext().setAuthentication(loginUser);
}
